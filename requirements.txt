Django>=5.0,<6.0
psycopg[binary]>=3.1
mysql-connector-python>=8.0
celery>=5.4
redis>=5.0
python-dotenv>=1.0
# WebSocket support with Django Channels
channels>=4.0
channels-redis>=4.1
daphne>=4.0
# Pin cbor2 to a version with prebuilt wheels (6.x ships sdist-only on macOS and needs a Rust toolchain to build)
cbor2==5.6.5
# PDF rendering: install WeasyPrint on Linux/Ubuntu deployments (Windows dev often skips it).
weasyprint>=62.0
Pillow
# QR code generation
qrcode[pil]>=8.0
# REST API for Flutter mobile app
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
cryptography>=41.0
requests>=2.31
gunicorn>=21.2
django-celery-beat>=2.9.0
sentry-sdk[django]>=2.0

django-cotton>=2.7,<3.0
